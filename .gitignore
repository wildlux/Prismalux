# ── Ambiente virtuale Python ────────────────────────────────────────────────
.venv/
venv/
env/
py_env/
**/py_env/

# ── Cache Python ─────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd

# ── llama.cpp — repo clonato (si riclona dal llama Studio) ──────────────────
ENGINE_LLM/llama_cpp_studio/llama.cpp/
C_software/llama.cpp/

# ── C_software — binari e oggetti compilati ───────────────────────────────────
C_software/*.o
C_software/src/*.o
C_software/prismalux
C_software/prismalux_http
C_software/vram_bench
C_software/vram_bench_static
C_software/download_model
C_software.zip

# ── C_software — binari test compilati (tenere solo i .c sorgenti) ────────────
C_software/test_engine
C_software/test_guardia_math
C_software/test_math_locale
C_software/test_golden
C_software/test_toon_config
C_software/test_memory
C_software/test_sha256
C_software/test_version
C_software/test_stress
C_software/test_stress_asan
C_software/test_stress_bin
C_software/test_perf
C_software/test_cs_random
C_software/test_agent_scheduler
C_software/test_rag
C_software/test_rag_bench
C_software/test_multiagente
C_software/test_sim_smoke
C_software/test_hw_platform
C_software/test_security
C_software/jlt_demo
C_software/jlt_speed_test
C_software/jlt_speedup_demo

# ── Qt GUI — directory build (binari compilati) ────────────────────────────────
C_software/Qt_GUI/build/
C_software/Qt_GUI/build_dbg/
C_software/Qt_GUI/build_debug/
C_software/Qt_GUI/build_tests/
Qt_GUI/build/
Qt_GUI/build_dbg/
Qt_GUI_v3/build/
Qt_GUI_v3/build_dbg/
Qt_GUI_v3/build_tests/

# ── Cartelle root duplicate/sperimentali (non fanno parte del progetto principale)
# Nota: / iniziale ancora il pattern alla root del repo
/Qt_GUI/
/ENGINE_LLM/llama_cpp_studio/

# ── Artefatti generati e file personali ───────────────────────────────────────
Prismalux_Windows_full.zip
*.pdf
ENGINE_LLM/hybrid_llm/
C_software/C_software/
C_software/llama_cpp_studio/

# ── AppImage — artefatti generati ─────────────────────────────────────────────
*.AppImage
AppDir/

# ── Modelli GGUF personali (troppo grandi per git) ───────────────────────────
ENGINE_LLM/llama_cpp_studio/models/*.gguf
*.gguf

# ── Output Multi-Agente (generati a runtime, tutti in esportazioni/) ─────────
esportazioni/
multi_agente/output_*.py
multi_agente/report_*.html

# ── Cython build artifacts (solo in Python_Software/) ────────────────────────
*.so
Python_Software/**/*.c
Python_Software/**/*.html
build/
!Tools/build/
dist/
*.egg-info/
setup_cython.py

# ── Binari test con suffisso _bin e altri binari test standalone ──────────────
C_software/*_bin
C_software/test_bench

# ── Screenshot e foto personali ──────────────────────────────────────────────
photo_*.jpg
photo_*.png
screenshot_*.jpg
screenshot_*.png
EXPORT/assets/storico/

# ── File di sistema ───────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Log e file temporanei ─────────────────────────────────────────────────────
*.log
*.tmp
*.bak

# ── Memoria Claude (sessione locale, non versionare) ─────────────────────────
.claude/

# ── Memoria persistente agenti ────────────────────────────────────────────────
~/.multi_agente_memoria.json
~/.multi_agente_llm_db.json

# ── Dati utente sensibili ─────────────────────────────────────────────────────
strumento_pratico/cv/*.pdf
strumento_pratico/cv/*.txt

# ── Qt GUI — directory build ──────────────────────────────────────────────────
gui/build/
gui/build_*/

# ── llama.cpp standalone in ENGINE_LLM ────────────────────────────────────────
/ENGINE_LLM/llama.cpp/

# ── Binari whisper Windows in Frameworks/whisper/bin_windows ──────────────────
Frameworks/whisper/bin_windows/

# ── OpenCV — build artifacts (compilati localmente) ───────────────────────────
Frameworks/opencv/build/
Frameworks/opencv/bin_windows/

# ── FFmpeg — build artifacts (compilati localmente) ───────────────────────────
Frameworks/ffmpeg/build/
Frameworks/ffmpeg/bin_windows/

# ── ONNX Runtime — build artifacts (compilati localmente) ────────────────────
Frameworks/onnx/build/
Frameworks/onnx/bin_windows/

# ── Configurazione MCP locale ─────────────────────────────────────────────────
.mcp.json

# ── Screenshot ────────────────────────────────────────────────────────────────
Schermata_*.png

# ── Artefatti ZIP di distribuzione (troppo grandi per git) ───────────────────
Prismalux_v*.zip
ANDROID/*.zip

# ── Tools/compile_win — toolchain scaricata (troppo grande) e output build ───
Tools/compile_win/toolchain/
Tools/compile_win/build/

# ── KNOWLEDGE_USER — dati personali, non versionare ─────────────────────────
TOOL_TIP/KNOWLEDGE_USER/user_knowledge.md

# ── Screenshot e foto inviate/ricevute ───────────────────────────────────────
SCREENSHOT/

# ── Risultati benchmark / output temporanei ──────────────────────────────────
gui/risultati*.txt

# ── Android — artefatti build e firma ─────────────────────────────────────────
ANDROID/android_app/build-android/
ANDROID/android_app/build-host/
ANDROID/build_apk.sh
ANDROID/*.idsig
Tools/compile_win/.venv_tools/

# ── Build artifacts ───────────────────────────────────────────────────────────
build_gui/
build_tests/
Frameworks/whisper/

# ── DLL Windows bundlate (estratte da MSYS2, non versionare) ─────────────────
DLL/

# ── RAG — documenti personali locali (possono pesare GB) ─────────────────────
RAG/
RAG.zip
PARERE_RICERCA_BLHM_RAB80L.txt

# ── KNOWLEDGE_USER — tutti i file personali ───────────────────────────────────
TOOL_TIP/KNOWLEDGE_USER/

# ── Artefatti di distribuzione (rigenerati da aggiorna.sh) ───────────────────
EXPORT/linux/Prismalux*.AppImage
EXPORT/linux/Prismalux*.zip
EXPORT/windows/Prismalux*.zip
Prismalux_v*.zip
build_status.txt
MCPs/anki_mcp/node_modules/
MCPs/anki_mcp/package-lock.json

# ── Script di migrazione (eseguito una volta, non fa parte del repo) ─────────
gui/lan_web/extract_html.py

# ── Segreti — prevenzione commit accidentale ─────────────────────────────────
.env
*.env
*.key
*.pem
*.p12
