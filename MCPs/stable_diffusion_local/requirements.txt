# Stable Diffusion locale — dipendenze
# Installazione: pip install -r requirements.txt
# Per GPU NVIDIA (CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Per CPU/MPS:
#   pip install torch
# Versioni testate (2026-05-15)

diffusers>=0.27.0,<1.0
transformers>=4.40.0,<5.0
accelerate>=0.30.0,<2.0
torch>=2.2.0,<3.0
Pillow==12.1.1
