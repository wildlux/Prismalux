# RDKit MCP — dipendenze
# Installazione: pip install -r requirements.txt
# Alternativa conda: conda install -c conda-forge rdkit
# Alternativa apt:   sudo apt install python3-rdkit

rdkit>=2026.3.3
Pillow>=12.3.0
