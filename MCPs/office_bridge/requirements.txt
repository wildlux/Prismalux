# Office Bridge MCP — dipendenze
# Installazione: pip install -r requirements.txt
#
# Modalità UNO (LibreOffice diretto):
#   Linux: sudo apt install python3-uno libreoffice
#
# Modalità file (fallback senza LibreOffice):
-c ../constraints.txt
python-docx>=1.2.0
openpyxl>=3.1.5
python-pptx>=1.0.2
