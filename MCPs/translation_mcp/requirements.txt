# translation_mcp — stdlib only (urllib)
# Richiede LibreTranslate (locale o remoto):
#   pip install libretranslate
#   libretranslate --host 0.0.0.0 --port 5000
# Oppure: docker run -p 5000:5000 libretranslate/libretranslate
